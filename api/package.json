{
  "name": "corpocache-api",
  "version": "1.0.0",
  "description": "Azure Functions API for CorpoCache personal finance dashboard",
  "main": "dist/src/functions/*.js",
  "scripts": {
    "build": "tsc",
    "watch": "tsc -w",
    "clean": "rimraf dist",
    "prestart": "npm run clean && npm run build",
    "start": "func start",
    "test": "jest"
  },
  "dependencies": {
    "@azure/functions": "^4.0.0",
    "mssql": "^10.0.0"
  },
  "devDependencies": {
    "@types/mssql": "^9.1.0",
    "@types/node": "^20.0.0",
    "azure-functions-core-tools": "^4.6.0",
    "rimraf": "^5.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
