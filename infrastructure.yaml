# Infrastructure Pattern Request for CorpoCache
# Generated by Infrastructure MCP Server v2.0
#
# Pattern: api-backend
# Category: composite
# Components: function-app, azure-sql, keyvault, security-groups, rbac-assignments, diagnostic-settings
#
# Estimated monthly cost: ~$5 (dev/small)
#
# What will be provisioned:
# - Resource Group: rg-corpocache-dev
# - Azure SQL Server + Database (Basic tier)
# - Key Vault for secrets (DB connection string, GitHub OAuth creds)
# - Function App (Node.js runtime, consumption plan)
# - Security Groups with owner delegation
# - RBAC assignments for all components
#
# Note: Frontend is deployed via Azure Static Web Apps (separate workflow)
# The Function App here can be used standalone or integrated with SWA
#

version: "1"
metadata:
  project: corpocache
  environment: dev
  business_unit: personal
  owners:
    - zerocool@github.com
  location: eastus

pattern: api-backend

config:
  name: corpocache
  size: small
  runtime: node
  databases:
    - corpocache
  enable_openapi: false
