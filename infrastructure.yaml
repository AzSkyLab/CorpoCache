# Infrastructure Pattern Request
# Generated by Infrastructure MCP Server v2.0
# Test run 20: testing additional keyvault resource
#
# Pattern: api-backend
# Category: composite
# Components: function-app, azure-sql, keyvault, security-groups, rbac-assignments, diagnostic-settings
#
# Additional Resources:
# - keyvault (corpocache-secrets)
#
# Estimated monthly cost: $10
# Size: small
#
# What will be provisioned:
# - Resource Group: rg-corpocache-dev
# - Security Groups with owner delegation
# - RBAC assignments for all components
# - Key Vault with secrets management (primary)
# - Key Vault: corpocache-secrets (additional)
# - Diagnostic settings (staging/prod)
#

version: "1"
metadata:
  project: corpocache
  environment: dev
  business_unit: personal
  owners:
    - sa_scottc1@azureskylab.com
  location: eastus2
pattern: api-backend
config:
  name: corpocache
  runtime: node
  databases:
    - corpocache
  enable_openapi: false
  size: small

---
# Second Key Vault for additional secrets
version: "1"
metadata:
  project: corpocache
  environment: dev
  business_unit: personal
  owners:
    - sa_scottc1@azureskylab.com
  location: eastus2
pattern: keyvault
config:
  name: corpocache-secrets
  size: small
