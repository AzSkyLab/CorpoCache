# Infrastructure Pattern Request - Access Review Test
# Test run 23: fixed pattern config to include enable_access_review in optional
#
# This tests the keyvault pattern with access reviews enabled

version: "1"
metadata:
  project: corpocache
  environment: dev
  business_unit: personal
  owners:
    - sa_scottc1@azureskylab.com
  location: eastus2
pattern: keyvault
config:
  name: corpocache-secrets
  size: small
  enable_access_review: true
