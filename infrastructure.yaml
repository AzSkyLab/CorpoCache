# Infrastructure Pattern Request
# Generated by Infrastructure MCP Server v2.0
# Test run 23: validating multi-document infrastructure workflow
#
# Action: CREATE
# Patterns: api-backend + keyvault (secondary)
#
# Estimated monthly cost: ~$10 (dev)
#
# What will be provisioned:
# - Resource Group: rg-corpocache-dev
# - Azure Function App (Node.js, Consumption plan)
# - Azure SQL Database (Basic tier, corpocache db)
# - Key Vault #1 (for SQL connection string - included in api-backend)
# - Key Vault #2 (secondary, for additional secrets)
# - Security Groups with owner delegation
# - RBAC assignments for all components
#

version: "1"
metadata:
  project: corpocache
  environment: dev
  business_unit: personal
  owners:
    - sa_scottc1@azureskylab.com
  location: eastus2
pattern: api-backend
config:
  name: corpocache
  runtime: node
  databases:
    - corpocache
  enable_openapi: false
  size: small

---
# Secondary Key Vault
version: "1"
metadata:
  project: corpocache
  environment: dev
  business_unit: personal
  owners:
    - sa_scottc1@azureskylab.com
  location: eastus2
pattern: keyvault
config:
  name: corpocache-secrets
  size: small
