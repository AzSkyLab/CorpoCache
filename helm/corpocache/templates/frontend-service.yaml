apiVersion: v1
kind: Service
metadata:
  name: {{ include "corpocache.fullname" . }}-frontend
  labels:
    {{- include "corpocache.frontend.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "corpocache.frontend.selectorLabels" . | nindent 4 }}
